# ============================================
# AI Research App - 环境变量配置示例
# 复制此文件为 .env 用于本地开发
# 生产环境请在 Zeabur/Railway 等平台设置
# ============================================

# ============================================
# ⚠️ 安全警告 - 请仔细阅读
# ============================================
# 1. 以 VITE_ 开头的变量会暴露给前端浏览器！
# 2. 切勿将 AI 模型 API Key 设为 VITE_ 变量
# 3. 切勿提交包含真实 API Key 的 .env 文件到 Git
# 4. AI 模型 API Key 只应在后端使用（server/目录）
# ============================================

# --------------------------------------------
# 前端暴露变量（不要包含敏感信息！）
# --------------------------------------------
# 这些变量会被打包到前端代码中，任何人都可以看到
VITE_APP_ID=your_app_id_here

# --------------------------------------------
# 应用基础配置（后端专用，安全）
# --------------------------------------------
# JWT 加密密钥 - 生产环境请使用随机生成的长字符串 (至少32位)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# 管理员 OpenID - 拥有管理员权限的用户标识
OWNER_OPEN_ID=

# --------------------------------------------
# 数据库配置（后端专用，安全）
# --------------------------------------------
# 本地开发使用文件路径
DATABASE_URL=file:./data/sqlite.db

# Zeabur 生产环境配置（挂载卷路径）
# DATABASE_URL=file:/app/data/sqlite.db

# Railway 生产环境配置
# DATABASE_URL=file:/railway/volume/sqlite.db

# --------------------------------------------
# AI 模型 API 配置（后端专用，切勿暴露给前端！）
# --------------------------------------------
# ⚠️ 警告：这些 API Key 只应在后端使用
# 切勿以 VITE_ 开头命名这些变量
#
# 支持以下模型（配置至少一个）：
# - OpenAI (推荐，最稳定): gpt-4o-mini, gpt-4o
# - Gemini: gemini-1.5-pro, gemini-1.5-flash
# - Kimi: moonshot-v1-8k
# - Qwen: qwen-turbo
# - Deepseek: deepseek-chat
#
# 默认使用 OpenAI，如果没有则使用第一个配置的模型

# OpenAI API（推荐，最稳定）- 获取地址: https://platform.openai.com
OPENAI_API_KEY=sk-your_openai_api_key_here
OPENAI_API_URL=https://api.openai.com/v1

# Gemini API（Google）- 获取地址: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta

# Kimi API（Moonshot）- 获取地址: https://platform.moonshot.cn
KIMI_API_KEY=sk-your_kimi_api_key_here
KIMI_API_URL=https://api.moonshot.cn/v1

# Qwen API（阿里云）- 获取地址: https://dashscope.aliyun.com
QWEN_API_KEY=sk-your_qwen_api_key_here
QWEN_API_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Deepseek API - 获取地址: https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-your_deepseek_api_key_here
DEEPSEEK_API_URL=https://api.deepseek.com/v1

# 默认使用模型（可选，不配置则使用第一个可用的）
DEFAULT_MODEL=gpt-4o-mini

# 是否启用自动故障转移（默认: true）
ENABLE_AUTO_FAILOVER=true

# --------------------------------------------
# OAuth 认证配置（可选）
# --------------------------------------------
# OAuth 服务器地址 - 本地开发使用默认值
OAUTH_SERVER_URL=http://localhost:3000/api/oauth

# 生产环境 OAuth 配置
# OAUTH_SERVER_URL=https://your-domain.com/api/oauth

# --------------------------------------------
# 网络和代理配置（可选）
# --------------------------------------------
# HTTP/HTTPS 代理 - 仅在需要时设置
# 注意: Zeabur/Railway 等海外服务器通常不需要设置
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890

# --------------------------------------------
# 高级配置（可选）
# --------------------------------------------
# 管理员绕过认证（仅用于开发测试）
# ⚠️ 生产环境切勿启用
# ADMIN_BYPASS=true

# 日志级别（debug, info, warn, error）
LOG_LEVEL=info

# 最大请求体大小（MB）
MAX_BODY_SIZE=50

# --------------------------------------------
# Stripe 支付配置（可选 - 如启用付费功能）
# --------------------------------------------
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PRICE_ID_PRO=price_...
# STRIPE_PRICE_ID_MAX=price_...

# --------------------------------------------
# 外部服务配置（可选）
# --------------------------------------------
# S3 存储配置（用于文件上传）
# S3_ENDPOINT=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_BUCKET=
# S3_REGION=

# --------------------------------------------
# 已移除/变更的配置
# --------------------------------------------
# ❌ BUILT_IN_FORGE_API_KEY - 已移除
#    原因: forge.manus.im 域名已不存在
#    请改用: OPENAI_API_KEY 或其他模型
#
# ❌ VITE_FRONTEND_FORGE_API_KEY - 已删除
#    原因：此变量会将 API Key 暴露给前端浏览器
